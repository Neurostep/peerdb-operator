---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: peerdbclusters.peerdb.peerdb.io
spec:
  group: peerdb.peerdb.io
  names:
    kind: PeerDBCluster
    listKind: PeerDBClusterList
    plural: peerdbclusters
    singular: peerdbcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.upgrade.phase
      name: Upgrade
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          PeerDBCluster is the Schema for the peerdbclusters API.
          It manages PeerDB's control plane components including Flow API, PeerDB Server, and PeerDB UI.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PeerDBCluster
            properties:
              components:
                description: components configures the PeerDB control plane components.
                properties:
                  authProxy:
                    description: authProxy configures the optional authentication
                      proxy.
                    properties:
                      credentials:
                        description: credentials configures the authentication credentials
                          for the proxy.
                        properties:
                          passwordSecretRef:
                            description: passwordSecretRef references the Secret containing
                              the auth proxy password.
                            properties:
                              key:
                                description: key is the key within the Secret.
                                minLength: 1
                                type: string
                              name:
                                description: name is the name of the Secret.
                                minLength: 1
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          username:
                            description: username is the auth proxy username.
                            minLength: 1
                            type: string
                        required:
                        - passwordSecretRef
                        - username
                        type: object
                      image:
                        description: image overrides the default Auth Proxy container
                          image.
                        type: string
                      replicas:
                        default: 1
                        description: replicas is the number of Auth Proxy replicas.
                        format: int32
                        minimum: 0
                        type: integer
                      resources:
                        description: resources defines compute resource requirements
                          for the Auth Proxy container.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: service configures the Auth Proxy Kubernetes
                          Service.
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: annotations are additional annotations to
                              apply to the Service.
                            type: object
                          port:
                            description: port is the port the Service listens on.
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          type:
                            default: ClusterIP
                            description: type is the Kubernetes Service type.
                            enum:
                            - ClusterIP
                            - LoadBalancer
                            type: string
                        type: object
                    required:
                    - credentials
                    type: object
                  flowAPI:
                    description: flowAPI configures the Flow API component.
                    properties:
                      image:
                        description: image overrides the default Flow API container
                          image.
                        type: string
                      replicas:
                        default: 1
                        description: replicas is the number of Flow API replicas.
                        format: int32
                        minimum: 0
                        type: integer
                      resources:
                        description: resources defines compute resource requirements
                          for the Flow API container.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: service configures the Flow API Kubernetes Service.
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: annotations are additional annotations to
                              apply to the Service.
                            type: object
                          port:
                            description: port is the port the Service listens on.
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          type:
                            default: ClusterIP
                            description: type is the Kubernetes Service type.
                            enum:
                            - ClusterIP
                            - LoadBalancer
                            type: string
                        type: object
                    type: object
                  peerDBServer:
                    description: peerDBServer configures the PeerDB Server component.
                    properties:
                      image:
                        description: image overrides the default PeerDB Server container
                          image.
                        type: string
                      passwordSecretRef:
                        description: passwordSecretRef references the Secret containing
                          the PeerDB Server password.
                        properties:
                          key:
                            description: key is the key within the Secret.
                            minLength: 1
                            type: string
                          name:
                            description: name is the name of the Secret.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      replicas:
                        default: 1
                        description: replicas is the number of PeerDB Server replicas.
                        format: int32
                        minimum: 0
                        type: integer
                      resources:
                        description: resources defines compute resource requirements
                          for the PeerDB Server container.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: service configures the PeerDB Server Kubernetes
                          Service.
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: annotations are additional annotations to
                              apply to the Service.
                            type: object
                          port:
                            description: port is the port the Service listens on.
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          type:
                            default: ClusterIP
                            description: type is the Kubernetes Service type.
                            enum:
                            - ClusterIP
                            - LoadBalancer
                            type: string
                        type: object
                    type: object
                  ui:
                    description: ui configures the PeerDB UI component.
                    properties:
                      image:
                        description: image overrides the default PeerDB UI container
                          image.
                        type: string
                      nextAuthSecretRef:
                        description: nextAuthSecretRef references the Secret containing
                          the NextAuth secret for session signing.
                        properties:
                          key:
                            description: key is the key within the Secret.
                            minLength: 1
                            type: string
                          name:
                            description: name is the name of the Secret.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      nextAuthURL:
                        default: http://localhost:3000
                        description: nextAuthURL is the canonical URL for NextAuth.js
                          (e.g., "http://localhost:3000").
                        type: string
                      passwordSecretRef:
                        description: passwordSecretRef references the Secret containing
                          the UI login password.
                        properties:
                          key:
                            description: key is the key within the Secret.
                            minLength: 1
                            type: string
                          name:
                            description: name is the name of the Secret.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      replicas:
                        default: 1
                        description: replicas is the number of PeerDB UI replicas.
                        format: int32
                        minimum: 0
                        type: integer
                      resources:
                        description: resources defines compute resource requirements
                          for the PeerDB UI container.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      service:
                        description: service configures the PeerDB UI Kubernetes Service.
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: annotations are additional annotations to
                              apply to the Service.
                            type: object
                          port:
                            description: port is the port the Service listens on.
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          type:
                            default: ClusterIP
                            description: type is the Kubernetes Service type.
                            enum:
                            - ClusterIP
                            - LoadBalancer
                            type: string
                        type: object
                    type: object
                type: object
              dependencies:
                description: dependencies defines external systems required by PeerDB.
                properties:
                  catalog:
                    description: catalog is the PostgreSQL catalog database connection
                      configuration.
                    properties:
                      database:
                        description: database is the name of the catalog database.
                        minLength: 1
                        type: string
                      host:
                        description: host is the hostname of the PostgreSQL server.
                        minLength: 1
                        type: string
                      passwordSecretRef:
                        description: passwordSecretRef references the Secret containing
                          the PostgreSQL password.
                        properties:
                          key:
                            description: key is the key within the Secret.
                            minLength: 1
                            type: string
                          name:
                            description: name is the name of the Secret.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      port:
                        default: 5432
                        description: port is the port of the PostgreSQL server.
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      sslMode:
                        default: require
                        description: sslMode is the PostgreSQL SSL connection mode.
                        enum:
                        - disable
                        - require
                        - verify-ca
                        - verify-full
                        type: string
                      user:
                        description: user is the PostgreSQL user for catalog access.
                        minLength: 1
                        type: string
                    required:
                    - database
                    - host
                    - passwordSecretRef
                    - user
                    type: object
                  temporal:
                    description: temporal is the Temporal server connection configuration.
                    properties:
                      address:
                        description: address is the host:port of the Temporal frontend
                          service.
                        minLength: 1
                        type: string
                      namespace:
                        description: namespace is the Temporal namespace used by PeerDB.
                        minLength: 1
                        type: string
                      tlsSecretRef:
                        description: |-
                          tlsSecretRef optionally references a Secret containing TLS credentials for Temporal.
                          The Secret should contain "tls.crt" and "tls.key" entries.
                        properties:
                          key:
                            description: key is the key within the Secret.
                            minLength: 1
                            type: string
                          name:
                            description: name is the name of the Secret.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    required:
                    - address
                    - namespace
                    type: object
                required:
                - catalog
                - temporal
                type: object
              imagePullSecrets:
                description: imagePullSecrets is a list of references to Secrets for
                  pulling container images.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              init:
                description: init configures the init jobs that run during cluster
                  setup.
                properties:
                  temporalNamespaceRegistration:
                    description: temporalNamespaceRegistration configures the job
                      that registers the Temporal namespace.
                    properties:
                      backoffLimit:
                        default: 4
                        description: backoffLimit is the number of retries before
                          marking the job as failed.
                        format: int32
                        minimum: 0
                        type: integer
                      enabled:
                        default: true
                        description: enabled controls whether this init job runs.
                        type: boolean
                      image:
                        description: image overrides the default container image for
                          the init job.
                        type: string
                      resources:
                        description: resources defines compute resource requirements
                          for the init job container.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                    type: object
                  temporalSearchAttributes:
                    description: temporalSearchAttributes configures the job that
                      registers Temporal search attributes.
                    properties:
                      backoffLimit:
                        default: 4
                        description: backoffLimit is the number of retries before
                          marking the job as failed.
                        format: int32
                        minimum: 0
                        type: integer
                      enabled:
                        default: true
                        description: enabled controls whether this init job runs.
                        type: boolean
                      image:
                        description: image overrides the default container image for
                          the init job.
                        type: string
                      resources:
                        description: resources defines compute resource requirements
                          for the init job container.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                    type: object
                type: object
              maintenance:
                description: |-
                  maintenance configures PeerDB maintenance mode for graceful upgrades.
                  When configured, the operator runs maintenance workflows to pause mirrors
                  before upgrading and resume them after.
                properties:
                  backoffLimit:
                    default: 4
                    description: backoffLimit is the number of retries before marking
                      the maintenance job as failed.
                    format: int32
                    minimum: 0
                    type: integer
                  image:
                    description: image overrides the default flow-maintenance container
                      image.
                    type: string
                  resources:
                    description: resources defines compute resource requirements for
                      the maintenance job container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              maintenanceWindow:
                description: |-
                  maintenanceWindow defines a daily time window during which upgrades may start.
                  Only used when upgradePolicy is Automatic.
                properties:
                  end:
                    description: end is the end of the maintenance window in HH:MM
                      (24h) format.
                    pattern: ^([01]\d|2[0-3]):[0-5]\d$
                    type: string
                  start:
                    description: start is the beginning of the maintenance window
                      in HH:MM (24h) format.
                    pattern: ^([01]\d|2[0-3]):[0-5]\d$
                    type: string
                  timeZone:
                    description: |-
                      timeZone is an IANA timezone name (e.g. "UTC", "America/Los_Angeles").
                      Defaults to UTC if not specified.
                    type: string
                required:
                - end
                - start
                type: object
              paused:
                description: paused stops reconciliation of this PeerDBCluster when
                  set to true.
                type: boolean
              serviceAccount:
                description: serviceAccount configures the ServiceAccount used by
                  PeerDB components.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: annotations are additional annotations to apply to
                      the ServiceAccount.
                    type: object
                  create:
                    default: true
                    description: create indicates whether the operator should create
                      a ServiceAccount.
                    type: boolean
                type: object
              upgradePolicy:
                allOf:
                - enum:
                  - Automatic
                  - Manual
                - enum:
                  - Automatic
                  - Manual
                default: Automatic
                description: upgradePolicy controls whether upgrades are applied automatically
                  or require manual approval.
                type: string
              version:
                description: version is the PeerDB image tag to deploy (e.g. "v0.36.7").
                minLength: 1
                type: string
            required:
            - dependencies
            - version
            type: object
          status:
            description: status defines the observed state of PeerDBCluster
            properties:
              conditions:
                description: conditions represent the current state of the PeerDBCluster.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoints:
                description: endpoints reports the addresses of PeerDB endpoints.
                properties:
                  flowAPIAddress:
                    description: flowAPIAddress is the address of the Flow API endpoint.
                    type: string
                  serverAddress:
                    description: serverAddress is the address of the PeerDB Server
                      endpoint.
                    type: string
                  uiAddress:
                    description: uiAddress is the address of the PeerDB UI endpoint.
                    type: string
                type: object
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              upgrade:
                description: upgrade tracks the progress of a version upgrade.
                properties:
                  fromVersion:
                    description: fromVersion is the version the cluster is upgrading
                      from.
                    type: string
                  message:
                    description: message is a human-readable description of the current
                      upgrade state.
                    type: string
                  phase:
                    description: phase is the current phase of the upgrade.
                    type: string
                  startedAt:
                    description: startedAt is the time the upgrade was started.
                    format: date-time
                    type: string
                  toVersion:
                    description: toVersion is the version the cluster is upgrading
                      to.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
